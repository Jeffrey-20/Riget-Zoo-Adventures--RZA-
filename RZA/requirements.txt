
# Core framework
Django>=5.0,<6.0

# Database driver (choose one depending on your DB)
psycopg2-binary>=2.9,<3.0   # for PostgreSQL
# mysqlclient>=2.2,<3.0     # for MySQL/MariaDB (uncomment if needed)

# Environment variable management
python-decouple>=3.8

# REST API support
djangorestframework>=3.15,<4.0

# Authentication & security
django-allauth>=0.63,<1.0
djangorestframework-simplejwt>=5.4,<6.0

# CORS and security headers
django-cors-headers>=4.3,<5.0

# Static/media files (if using cloud storage)
boto3>=1.34,<2.0            # AWS S3
django-storages>=1.14,<2.0

# Gunicorn (for deployment, e.g. on Linux servers)
gunicorn>=21.2,<22.0

# Testing & linting
pytest>=8.0,<9.0
pytest-django>=4.8,<5.0

# Optional: for form handling and UI
django-crispy-forms>=2.1,<3.0
crispy-bootstrap5>=0.7,<1.0
